[project]
name = "twa-backend"
version = "0.1.0"
description = "FastAPI backend for the TWA application"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn[standard]>=0.35.0",
]

[tool.uv]
package = false
