services:
  auth-service:
    build:
      context: ${AUTHSDK_PATH:-../authSDK-1.1.0}
      dockerfile: docker/Dockerfile
    image: auth-service:local
    pull_policy: never

  auth-webhook-worker:
    image: auth-service:local
    pull_policy: never

  auth-webhook-scheduler:
    image: auth-service:local
    pull_policy: never